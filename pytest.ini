[pytest]
pythonpath = . Backend
testpaths = tests
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests requiring a running backend
    model: marks tests that download large ML models
